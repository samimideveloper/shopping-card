import { route } from "react-route-type";

export const Routes = {
	homepage: route(["homepage"]),
	cart: route(["cart"]),
	checkout:route(["checkout"]),
	login:route(["login"]),
	signup:route(["signup"])
};
